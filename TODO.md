# TODO
- Switch to persisted state.
- Remove all the old code so resumption works by just reading persisted state.
- See if we can make proxies faster?
- Ensure filters work
- Maybe make config file setup (filters, proxy)
- Make sure wait and max connections work
- Rip out webp stuff
